$("#<%= "favorite_form-#{ @post.id }"%>").html("<%= escape_javascript(
                                            render('shared/favorite',
                                                    { feed_item: @post })) %>");