<div class="main sign-up">
  <div class="container">
    <div class="row">
      <div class="col-md-4 offset-md-4">
        <div class="sign-up">
          <div class="sign-form sign-card">
            <h1>InstaClone</h1>
            <h2>登録して友達の写真をチェックしよう</h2>
            <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
              <%= render "devise/shared/error_messages", resource: resource %>
              
              <div class="field">
                <%= f.label "フルネーム" %><br />
                <%= f.text_field :fullname, autofocus: true, autocomplete: "fullname" %>
              </div>
              
              <div class="field">
                <%= f.label "ユーザーネーム" %><br />
                <%= f.text_field :username, autofocus: true, autocomplete: "username" %>
              </div>
            
              <div class="field">
                <%= f.label :email %><br />
                <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
              </div>
            
              <div class="field">
                <%= f.label :password %>
                <% if @minimum_password_length %>
                <em>(<%= @minimum_password_length %> characters minimum)</em>
                <% end %><br />
                <%= f.password_field :password, autocomplete: "new-password" %>
              </div>
            
              <div class="field">
                <%= f.label :password_confirmation %><br />
                <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
              </div>
            
              <div class="actions">
                <%= f.submit "登録する", class: "btn btn-primary btn-block" %>
              </div>
              
              <p class="term">登録することで、Facebookの<%= link_to "利用規約", '#', class: "link" %>,
              <%= link_to "データに関するポリシー", '#', class: "link" %>
              <%= link_to "cookieポリシー", '#', class: "link" %>に同意するものとします。</p>
            <% end %>
          </div>
          <div class="sign-card">
            <%= render "devise/shared/links" %>  
          </div>  
        </div>
      </div>
    </div>
  </div>  
</div>
